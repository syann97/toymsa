server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:  # default
          uri: file:///D:/Seyoung/DEV/Spring/git-local-repo
          default-label: master