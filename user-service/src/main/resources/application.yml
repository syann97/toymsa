server:
  port: 0

spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://127.0.0.1:8888
  profiles:
    active: native
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  datasource:
    url: jdbc:mysql://localhost:3306/msa_user_service
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: $2a$10$.flXiAvnQ6V7wzzky9WFbu0NR95uwIYJtlJbSZ7/h81byzxEAWdou
  cloud:
    config:
      name: application



eureka:
  instance:
    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka


greeting:
  message: Welcome to the Simple E-commerce. (spring boot)


#token:
#  secret: my-very-long-secret-key-1234567890123456
#  expiration-time: 86400000 # 10 days

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health
          - beans
          - info
          - busrefresh
